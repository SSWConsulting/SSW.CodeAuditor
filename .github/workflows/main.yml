# This is a basic workflow to help you get started with Actions
name: CI

# Controls when the action will run. Triggers the workflow on push or pull request
# events but only for the master branch
on:
  push:
    branches: [main]

# A workflow run is made up of one or more jobsjj that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      - uses: actions/checkout@v2
      - name: Run a multi-line script
        env:
          FIREBASETOKEN: ${{ secrets.FIREBASETOKEN }}
        run: |
          cd ui
          npm install
          npm run deploy
          cd ../api/functions
          npm install
          firebase deploy --token 1//0gSqqX2IdfHAJCgYIARAAGBASNwF-L9IrYDd4hFcDtMEWrQxRnXiKT5VSV5gDE-6ghmLe3UVVDB94veIIOMtbNyM7qbWIUhtyBWc --only functions
